<% # danger, info, warning, success %>
<% flash.each do |type, msg| %>
  <div class="alert alert-#{type}">
    <a class="close" date-dismiss='alert'> &#215;
    <%= content_tag :div, msg, id: "flash_#{type}" if msg.is_a?(String) %>
<% end %>
